{
  "name": "compassai",
  "private": true,
  "workspaces": [
    "AIHub_Frontend"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace AIHub_Frontend run dev",

    "dev:backend:win": "dotenv -e .env -- cross-env-shell \"cd AIHub_Backend && gradlew.bat bootRun\"",
    "dev:backend:unix": "dotenv -e .env -- sh -c 'cd AIHub_Backend && ./gradlew bootRun'",

    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm:dev:backend:win\" \"npm:dev:frontend\"",
    "dev:unix": "concurrently -k -n backend,frontend -c blue,green \"npm:dev:backend:unix\" \"npm:dev:frontend\"",

    "build:frontend": "npm --workspace AIHub_Frontend run build",
    "build:backend:win": "dotenv -e .env -- cross-env-shell \"cd AIHub_Backend && gradlew.bat build\"",
    "build:backend:unix": "dotenv -e .env -- sh -c 'cd AIHub_Backend && ./gradlew build'",
    "build": "npm run build:frontend && npm run build:backend:win",

    "clean": "npm --workspace AIHub_Frontend run clean || echo ok"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0",
    "dotenv-cli": "^10.0.0"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  }
}
